meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{urlApi}}/v1/login
  body: json
  auth: inherit
}

body:json {
  {
    "userName": "miguelito",
    "password": "sdlkfj298LJ!"
  }
}

script:post-response {
  const response = res.getBody();
  bru.setEnvVar("auth_token", response.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
